# Curated default modality skip patterns for ocprobe validate
# One pattern per line. Comments start with #.
# Globs are case-sensitive; * matches any chars including /; ? = single char.
# See lib/validate.sh:validate_glob_match() for matching semantics.

# Embeddings / text encoders
*embed*
*embedding*

# Reranking / cross-encoders
*rerank*
*reranker*
*cross-encoder*

# Text-to-speech / audio generation
*tts*
*speech*
*voice*
*whisper*

# Image generation / vision
*dall-e*
*midjourney*
*stable-diffusion*
*sdxl*
*image-gen*
*lyria*
*imagen*
*flux*

# Video generation
*video*
*sora*
*runway*
*pika*
*luma*
*veo*

# Multimodal / omni / live / realtime session models
# These accept text but do not complete a one-shot text turn reliably, so a
# text probe produces false failures (observed: *-omni-*, *-live-*).
*-omni-*
*omni-*
*-live-*
*-live$
*-realtime-*

# Audio / transcription / translation (non text-output)
*transcribe*
*translation*
*translate*
*-audio-*

# Moderation / safety classifiers
*moderation*
*safety*
*classifier*
*guard*
*content-filter*

# Specialized / non-chat
*code-interpreter*
